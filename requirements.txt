# Pure-pip dependencies (installed via `pip install -r requirements.txt`).
# torch and llama-cpp-python are installed separately by setup.sh/setup.bat
# because they need hardware-specific package indexes.
customtkinter
pillow
requests
numpy
ultralytics
huggingface-hub
opencv-python
